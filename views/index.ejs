<%- include('partials/header') %>

<style>
   body {
      min-height: 100vh;
    
      background: linear-gradient(45deg, #0f172a, #1e293b);
    }

    .home-container {
      position: relative;
     
      padding: 4rem;
      background: linear-gradient(
        135deg,
        rgba(16, 20, 54, 0.9) 0%,
        rgba(8, 11, 36, 0.9) 100%
      );
      border-radius: 30px;
      transform-style: preserve-3d;
      overflow: hidden;
    }

    .home-container::before {
      content: '';
      position: absolute;
      top: -50%;
      left: -50%;
      width: 200%;
      height: 200%;
      background: linear-gradient(
        45deg,
        transparent 20%,
        rgba(59, 130, 246, 0.1) 35%,
        rgba(147, 51, 234, 0.1) 65%,
        transparent 80%
      );
      animation: rotate 20s linear infinite;
      pointer-events: none;
    }

    @keyframes rotate {
      0% {
        transform: rotate(0deg);
      }
      100% {
        transform: rotate(360deg);
      }
    }

    .content {
      position: relative;
      z-index: 1;
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 2rem;
      margin-top: -150px;
    }

    h1 {
      font-size: 3.5rem;
      font-weight: 800;
      text-align: center;
      background: linear-gradient(135deg, #60a5fa, #8b5cf6, #ec4899);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      text-shadow: 0 0 30px rgba(96, 165, 250, 0.5);
      animation: titleGlow 3s ease-in-out infinite;
    }

    @keyframes titleGlow {
      0%, 100% {
        filter: brightness(1);
      }
      50% {
        filter: brightness(1.3);
      }
    }

    p {
      font-size: 1.25rem;
      line-height: 1.8;
      color: #94a3b8;
      text-align: center;
      max-width: 800px;
      text-shadow: 0 2px 4px rgba(0, 0, 0, 0.2);
      transform: translateZ(20px);
    }

    .cta-container {
      margin-top: 2rem;
      perspective: 1000px;
    }

    .cta-button {
      display: inline-block;
      padding: 1.25rem 3rem;
      font-size: 1.25rem;
      font-weight: 600;
      text-decoration: none;
      color: #fff;
      background: linear-gradient(135deg, #3b82f6, #8b5cf6);
      border-radius: 15px;
      transform-style: preserve-3d;
      transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
      position: relative;
      overflow: hidden;
      box-shadow: 0 0 30px rgba(59, 130, 246, 0.5),
                  0 0 60px rgba(139, 92, 246, 0.3);
    }

    .cta-button::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: linear-gradient(
        90deg,
        transparent,
        rgba(255, 255, 255, 0.3),
        transparent
      );
      transform: translateX(-100%);
      transition: transform 0.6s;
    }

    .cta-button:hover {
      transform: translateY(-5px) translateZ(30px);
      box-shadow: 0 10px 40px rgba(59, 130, 246, 0.6),
                  0 0 80px rgba(139, 92, 246, 0.4);
    }

    .cta-button:hover::before {
      transform: translateX(100%);
    }

    /* Decorative elements */
    .decorative-circle {
      position: absolute;
      border-radius: 50%;
      background: linear-gradient(135deg, rgba(59, 130, 246, 0.1), rgba(139, 92, 246, 0.1));
      filter: blur(40px);
      z-index: 0;
    }

    .circle-1 {
      width: 300px;
      height: 300px;
      top: -150px;
      left: -150px;
    }

    .circle-2 {
      width: 400px;
      height: 400px;
      bottom: -200px;
      right: -200px;
    }

    /* Responsive design */
    @media (max-width: 768px) {
      .home-container {
        padding: 2rem;
        margin: 1rem;
      }

      h1 {
        font-size: 2.5rem;
      }

      p {
        font-size: 1.1rem;
      }

      .cta-button {
        padding: 1rem 2rem;
        font-size: 1.1rem;
      }
    }
</style>

<div class="home-container">
  <div class="content">
    <h1>WS Support Portal</h1>
    <p>We’re here to help you with any issues related to your eBooks. Whether it’s a reading issue, a purchase, or anything else, creating a ticket is easy and quick!</p>
    <div class="cta-container">
      <a href="/create-ticket" class="cta-button">Create a Ticket</a>
    </div>
  </div>
</div>


